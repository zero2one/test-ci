sudo: false

language: php

php:
  - 5.6

before_install:
  - composer self-update
  - composer install

script:
  - phpunit

after_script:
  # Scrutinizer-CI.
  - wget https://scrutinizer-ci.com/ocular.phar
  - php ocular.phar code-coverage:upload --format=php-clover build/logs/clover.xml

  # Code Climate.
  - CODECLIMATE_REPO_TOKEN=77f6524e0b8479cb963cceee02ddd50ee9638bb3a5b325129668252c63c07c74 ./vendor/bin/test-reporter
